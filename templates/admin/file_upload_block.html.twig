{% extends sonata_block.templates.block_base %}

{% block block %}
{{ encore_entry_script_tags('file-upload') }}
<div class="box">
    <div class="box-header">
        <h3 class="box-title">{{ settings.title }}</h3>
    </div>

    <div class="box-body">
        <div id="app-file-upload">
            <file-upload></file-upload>
        </div>
    </div>
</div>
{% endblock %}
